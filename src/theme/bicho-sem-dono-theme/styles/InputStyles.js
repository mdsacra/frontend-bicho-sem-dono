
export const InputStyles = {
    // Styles for the base style
    baseStyle: {},
    // Styles for the size variations
    sizes: {},
    // Styles for the visual style variations
    variants: {
        bsd: {
            border: "1px solid",
            boxShadow: "0px 6px 11px -4px",
            borderColor: "#344459",
            bg: "#EDF2F7",
            _focusVisible: {
                bg: "white"
            }
        }
    },
    // The default `size` or `variant` values
    defaultProps: {},
  }